# 1. 이해

```
K개의 랜선이 있다
K개의 랜선을 잘라 N개의 랜선을 만들어야 한다
N개의 랜선을 만들었을 때 최대 랜선 길이를 구해야 한다

N개보다 많이 만드는 것도 N개를 만드는 것에 포함됨

1≤K≤10,000
1≤N≤1,000,000

예)
K = 4, N = 11
802
743
457
539
라고 하자
200cm로 자르면 11개를 만족하면서 가장 길게 자를 수 있다
```

# 2. 설계

```
Q. 하나씩 올려가면서 이 문제를 시간제한 안에 풀 수 있나?
A. 최댓값이 2^31-1이므로, 말도 안된다

-> Binary Search!!

Q. 어디서부터 어디까지를 범위로 두고 절반씩 나눠서 찾아야 할까?
A. 최솟값이랑 최댓값을 사이에 두고 자르면 되잖아
```

# 3. 주의

```
over flow : start+end가 int 최대 범위 보다 클 수 있잖아??
zero : mid를 구할 때 0이 되면 0으로 나누는 경우가 생길 수 있음
			 그리고 정답이 1인데 0으로 정답을 출력하고 끝낼 수도 있음
```