# 1. 이해

```
[d(n)]
- n + 각 자리수

[생성자]
- n을 d(n)의 생성자
- 생성자가 없는 숫자를 "셀프 넘버"

[출력]
10000보다 작거나 같은 셀프 넘버를 모두 출력하라
```

# 2. 설계

```
1. d(n)을 정의하자
	- n을 입력받고, 각 자리수를 더한다
	- 각 자리수를 더하는 방법은 10으로 계속 나누면서 나머지를 더해가면 된다
2. DAT 배열 생성하고 1부터 10000을 d(n)에 집어넣자
	d(n)값에 카운팅 해놓는다
3. DAT 배열값이 0인 넘이 셀프 넘버이다
```